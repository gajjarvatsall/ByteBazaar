import 'package:flutter/material.dart';

import '../options_screen/body.dart';

class OptionsScreen extends StatelessWidget {
  const OptionsScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
